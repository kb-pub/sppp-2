spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: postgres
    password:
#    driver-class-name: com.mysql.jdbc.Driver
    initialization-mode: always
    schema: schema.sql
    data: data.sql